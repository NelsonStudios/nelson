<?php
    $_product = $block->getProduct();
    $minimumOrder = $_product->getResource()->getAttribute('minimum_order')->getFrontend()->getValue($_product);
?>

<?php if (!empty($minimumOrder) && false): ?>
<div class="product-minimum-order">
    <h2 class="minimum-order">
        <span><?php /* @escapeNotVerified */ echo '[ ' . __($minimumOrder) . ' ]'; ?></span>
    </h2>
</div>
<?php endif ?>
